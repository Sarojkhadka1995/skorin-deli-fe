// import { Toaster } from "react-hot-toast";

import { Toaster } from "react-hot-toast";

export const ToasterComponent = () => {
  return (
    <Toaster
      toastOptions={{
        className: "",
      }}
    />
  );
};
